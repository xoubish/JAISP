# Checkpoints

This directory stores local training artifacts (model checkpoints, detector weights,
PSF diagnostics, etc.). These files are intentionally ignored by git.

If you want to share a specific checkpoint, copy it elsewhere or add a tracked
artifact in a dedicated release location.
