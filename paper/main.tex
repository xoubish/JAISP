% ====================================================================
% JAISP paper -- target: The Astrophysical Journal (ApJ)
% AASTeX v6.3.1.  Get the class from https://journals.aas.org/aastex-package-for-manuscripts/
% Build:  latexmk -pdf main.tex   (or pdflatex x3 + bibtex)
% ====================================================================
\documentclass[twocolumn]{aastex701}

\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\graphicspath{{figures/}}

% --- convenience macros ---
\newcommand{\jaisp}{\textsc{Jaisp}}
\newcommand{\arcsecpx}{\arcsec\,\mathrm{px}^{-1}}
\newcommand{\todo}[1]{\textcolor{red}{[TODO: #1]}}
\newcommand{\ipac}{\affiliation{Caltech/IPAC, Pasadena, CA, USA}}
\newcommand{\mas}{\ensuremath{\,\mathrm{mas}}}
\usepackage{xcolor}

\shorttitle{JAISP: A Rubin–Euclid Foundation Model}
\shortauthors{Hemmati et al.}

\begin{document}

\title{JAISP: A Joint Rubin–Euclid Foundation Model for Cross-Instrument Source Detection and Astrometry}

\author[0000-0003-2226-5395]{Shoubaneh Hemmati}
\ipac
\email{shemmati@caltech.edu}

\author[0000-0003-3367-3415]{George Helou}
\ipac
\email{}

\author[0000-0001-8792-3091]{Yu-Heng Lin}
\ipac
\email{}

\author[0000-0001-9674-1564]{Rachel Akeson}
\ipac
\email{}

\author[0000-0002-9382-9832]{Andreas Faisst}
\ipac
\email{}

\author[0000-0002-8963-8056]{Thomas Greene}
\ipac
\email{}

\author[0000-0003-4401-0430]{David Shupe}
\ipac
\email{}

\begin{abstract}
\end{abstract}

\keywords{Astrometry --- Surveys --- neural networks --- Astronomy data analysis}
% ====================================================================
% JAISP paper -- target: ApJ. Itemized outline (v2, in SH style).
% ====================================================================

\section{Introduction}
Cosmology has entered a regime where its hardest questions are limited by systematics rather than by statistics. The current generation of wide-field surveys are measuring the growth of structure and the expansion history precisely enough that the residual tensions between probes can no longer be dismissed as noise, yet cannot be confidently attributed to new physics rather than to unmodeled measurement error \citep{Rhodes2017}. Breaking this impasse depends on the next generation of surveys not merely collecting more photons, but controlling systematics far more tightly than any single survey can on its own. The Vera C. Rubin Observatory's Legacy Survey of Space and Time \citep[LSST;][]{Ivezic2019}, ESA's \textit{Euclid} mission \citep{Mellier2025}, and the \textit{Nancy Grace Roman Space Telescope} \citep{Spergel2015} will observe overlapping sky from ${\sim}0.3$ to 2~\micron\ at sub-arcsecond resolution. Individually powerful, in combination they can calibrate and cross-check one another, suppressing survey-specific systematics that no single survey can control alone \citep{Chary2020}. Exploiting that overlap at the pixel level, however, means confronting heterogeneous PSFs, samplings, and noise properties simultaneously, a regime where classical pipelines strain.

Machine and deep learning have moved quickly from peripheral tools to central ones in survey astronomy, and there is a growing case that much of the next decade of pixel-level processing will rely on learned components alongside explicitly modeled ones. That case should not be mistaken for a shortcut. The classical pipelines these surveys rely on represent decades of accumulated, problem-specific effort: careful PSF models, distortion solutions, deblenders, and uncertainty propagation, each refined against the failure modes of real data. Reaching, and then surpassing, that level of fidelity with learned methods requires comparable effort: the understanding encoded in the classical solution is precisely what tells us how to design, constrain, and validate its learned replacement. The payoff is a more data-driven pipeline: once that effort is invested, inference is fast, and the representation is shaped by the pixels themselves rather than by parametric assumptions, letting it absorb structure such as cross-instrument PSF differences, wavelength-dependent morphology, and correlated noise that classical stages can only approximate.

This does not mean every problem calls for a single, large foundation model. Much recent work comes instead from compact, purpose-built networks designed around a specific measurement and its systematics---generative and physics-informed models for deblending, super-resolution, and the realistic image simulations needed for shear calibration \citep[e.g.,][]{Hemmati2022, Haghjoo2026, Scognamiglio2025, Scognamiglio2026}---where the architecture follows directly from the problem. Foundation models occupy a complementary niche: rather than solving one task, they learn a general representation once through self-supervised pretraining and amortize it across many downstream tasks. This paradigm has begun to take hold in astronomy, through cross-modal and autoregressive models trained on large galaxy samples and the multi-survey datasets assembled to support them \citep[e.g.,][]{Parker_2024, Smith2024, MMU2024}. Building on this direction, we apply it to a problem central to joint survey processing: combining two imaging instruments at the pixel level, across mismatched resolutions and overlapping but non-identical bandpasses.

In this work we learn a single shared representation of Rubin and \textit{Euclid} imaging through self-supervised pretraining, and reuse it across several downstream measurements. We work from already-processed survey mosaics rather than individual exposures, which lets us concentrate on the joint representation itself; the same approach could in principle be pushed earlier in the pipeline, toward less-processed data where more of the instrumental signal survives. A foundation model is trained once to capture the cross-instrument spatial and spectral structure that joint processing requires; its frozen features then drive lightweight task-specific heads. We focus here on source detection and astrometry, benchmarked against classical baselines, and develop the photometry and shape-measurement heads in forthcoming work. Even at the mosaic level, we find that the learned representation transfers \textit{Euclid}'s sharp localization to Rubin's bands and to faint sources, improves source detection by combining all ten channels, and measures the relative astrometric offsets between two independently Gaia-aligned surveys. These results hold on a separate deep field with no retraining, a first test of the representation's transferability across the sky.

This paper is organized as follows. Section~\ref{sec:data} describes the Rubin and \textit{Euclid} imaging; Section~\ref{sec:architecture} presents the foundation model and the frozen-feature head design, and validates the learned representation; Sections~\ref{sec:detection} and~\ref{sec:astrometry} present the detection and astrometry heads; Section~\ref{sec:ood} tests transfer to a separate deep field; and Section~\ref{sec:discussion} discusses implications and next steps.


\section{Data}\label{sec:data}
\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/fig1_data.png}
\caption{\textbf{Data and survey footprints.}
    \textbf{(Left:)} The ECDFS field used for training, where both Euclid and Rubin provide overlapping coverage: a single Rubin tract (5063) spanning six patches,
    tiled into 790 $512\times512$ cutouts (stride 256\,px, 50\% overlap) over $\sim$0.2\,deg$^2$, shown on the Euclid VIS mosaic and tinted by patch. 
    \textbf{(Top-right:)} The EDF-S field, a widely separated region held out to test out-of-distribution transfer with no retraining: 72 tiles ($\sim$0.02\,deg$^2$).
    \textbf{(Bottom-right:)} The multi-waveband input channels for one matched tile---Rubin $ugrizy$ (gold
    labels) and Euclid VIS and NISP $Y$/$J$/$H$ (blue labels). Each panel is a $1'$ field. Rubin imaging is sampled at
    $0.2''$\,px$^{-1}$ and Euclid VIS/NISP channels are drawn from the MER mosaics, giving the joint Rubin$+$Euclid input stack used throughout this work.}
    \label{fig:data}
\end{figure*}

The dataset for this work combines overlapping imaging from Rubin and \textit{Euclid} over two widely separated Euclid Deep Fields. Training uses the Euclid Deep Field Fornax (EDF-F), which coincides with the Extended Chandra Deep Field South (ECDFS) in the Rubin footprint; the Euclid Deep Field South (EDF-S) is reserved entirely for the out-of-distribution transfer test of Section~\ref{sec:ood} and is never seen during training. For each field we assemble a common ten-band stack: the six Rubin optical bands $ugrizy$ together with \textit{Euclid} VIS and the three NISP near-infrared bands $Y$, $J$, and $H$ (Figure~\ref{fig:data}). All imaging is drawn from currently public data releases. The region used here is a small part of the available Rubin--\textit{Euclid} overlap: it is a single Rubin tract within one deep field, while the deep overlap spans several tracts per field and multiple fields, leaving substantial room to scale the same approach to larger areas as deeper releases arrive.

Rubin imaging is drawn from the Legacy Survey of Space and Time Data Preview 1 \citep[DP1;][]{DP1}, the first release from the Rubin Observatory. DP1 comprises coadded ugrizy imaging obtained with the Rubin Commissioning Camera (LSSTComCam) over seven non-contiguous fields during on-sky commissioning in late 2024, and we use the six-band coadds for the ECDFS field, the deepest and most uniformly observed of the seven \citep{DP1}. Within ECDFS we use a single Rubin tract (tract 5063), spanning six patches, which covers the region of common Rubin--Euclid depth used here. The coadds are sampled at $0.2\arcsecpx$; the median point-spread function is broad by space-based standards, with a full width at half maximum of roughly $1''$ at this early commissioning stage, so \textit{Euclid} VIS provides the sharp spatial reference throughout this work. The $5\sigma$ point-source coadd depths in this field are approximately $g,r \approx 26$, falling to $u\approx24.6$, and $y\approx23.1$; the full per-band values and processing details are given in the DP1 release paper.

\textit{Euclid} imaging is drawn from the Quick Data Release 1 \citep[Q1;][]{EuclidQ1}, the first public \textit{Euclid} release, which covers the three Euclid Deep Fields, including EDF-F and EDF-S used here. We use the visible channel VIS \citep{VIS} and the three NISP near-infrared bands $Y$, $J$, and $H$ \citep{NIR}, taken from the background-subtracted MER mosaics produced by the \textit{Euclid} MERge processing function \citep{MER}. All four bands are delivered on a common $0.1\arcsecpx$ grid, with VIS providing the highest resolution ($\sim0.16''$) and the NISP bands resampled onto the same grid; the $5\sigma$ point-source depths are $\approx26$ in VIS and $\approx24$ in each NISP band. Per-pixel variance maps accompany every band and are used for noise normalization throughout (Section~\ref{sec:architecture}).

Using the MER mosaics means inheriting that preprocessing: background subtraction, and in particular the resampling of the native near-infrared NISP frames onto the finer VIS pixel grid, are applied before our pipeline sees the data. This is a deliberate, conservative starting point that keeps the input fully reproducible from public products. Future \textit{Euclid} releases will provide the NISP mosaics at their native near-infrared resolution, which would let a joint model ingest them directly and perform the cross-instrument registration itself, reducing the preprocessing carried over from the survey pipeline; we return to this in Section~\ref{sec:discussion}.


For each field we lay the imaging out on a common tile grid. Rubin tiles are $512\times512$ pixels ($\sim102''$ on a side at $0.2\arcsecpx$), with the matching \textit{Euclid} region cut at twice the sampling; the ten bands of each tile are spatially aligned to a common frame. Tiles are placed on a $256$-pixel stride, so adjacent tiles overlap by $50\%$ and a given point on the sky falls in up to four tiles. The training field yields $790$ matched Rubin--\textit{Euclid} tiles over $\sim0.2$\,deg$^2$; the held-out EDF-S field provides a further $72$ matched tiles ($\sim0.02$\,deg$^2$), used only for the transfer test of Section~\ref{sec:ood}. This overlap is useful during pretraining, where the same source appears at different positions relative to the tile edge and so provides free augmentation, and during detection, where sources that sit near the edge of one tile fall near the center of a neighbor.

The overlap does, however, require care in evaluation. Because the tiles are cut from the same underlying coadd mosaics, the pixels shared by two overlapping tiles are identical rather than independent re-observations. A naive tile-level train/validation split would therefore place bit-identical copies of the same sky, carrying identical labels, on both sides of the split, making validation metrics artificially inflated. All held-out evaluations in this paper instead use a spatially disjoint split at the level of Rubin patches, so that no sky region and no source appears in both training and evaluation. We verified that results computed under this patch-disjoint protocol match those on the training field, confirming that the reported performance is not inflated by overlap leakage.

\section{AI Architecture}\label{sec:architecture}

The core of \jaisp{} is a single foundation model that learns a shared representation of the Rubin and \textit{Euclid} imaging, which is then reused by separate task-specific heads. The foundation model is trained by self-supervised masked-band reconstruction, a masked autoencoder \citep[MAE;][]{MAE} applied across wavelength bands rather than spatial patches: from the multi-band input, one band is withheld and the model is required to predict its image from the remaining ones. No labels are used at this stage, since the supervision comes entirely from the requirement that the model reproduce held-out pixels, so the model can be trained on raw imaging alone. Once trained, the encoder is frozen and its features drive the downstream heads. This work develops the detection and astrometry heads, with photometry and shape-measurement heads in preparation (Figure~\ref{fig:arch}).

\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/fig2_architecture.png}
    \caption{\textbf{Model architecture.}
    \textbf{(Top:)} Self-supervised pre-training by masked-band autoencoding. The ten input channels---Rubin $ugrizy$ (0.2$^{\prime\prime}$/px) and Euclid VIS\,$YJH$ (0.1$^{\prime\prime}$/px)---pass through per-band convolutional stems and a two-stream ConvNeXt encoder (Rubin and Euclid branches) whose feature maps shrink spatially and deepen in channels until they converge on a common 0.4$^{\prime\prime}$/px grid. A transformer bottleneck (4 layers, 8 heads, 256-d; $\sim$9.1M parameters) mixes the $\sim$128$^2$ tokens, and per-stream decoders reconstruct the single held-out band under an information-weighted loss. \textbf{(Bottom:)} For downstream tasks the encoder is frozen and its 0.4$^{\prime\prime}$/px latent features feed lightweight task heads---detection and
    astrometry in this work, with photometry and shape heads in preparation. Input and reconstruction thumbnails are the same matched ECDFS tile as Fig.~\ref{fig:data}.}
    \label{fig:arch}
\end{figure*}

The architecture must reconcile a mismatch between the instruments. Rubin imaging is sampled at $0.2\arcsecpx$ across six optical bands (ugrizy), whereas \textit{Euclid} VIS and the three NISP bands (Y, J, H) from MER are sampled twice as finely, at $0.1\arcsecpx$. Resampling both onto a single common grid would discard spatial information from the sharper instrument. The model instead keeps each instrument at its native resolution through separate encoder branches, and brings them together only once they reach a common physical scale.

Each band first passes through its own convolutional ``stem,'' a small network that maps a single noisy band into a shared feature space while normalizing for that band's noise level. The per-band features then enter one of two parallel encoder branches, one for Rubin and one for \textit{Euclid}, built from ConvNeXt blocks \citep[a modern convolutional design;][]{convnext}. Within each branch the feature maps are progressively reduced in spatial size and increased in depth, so that despite their different native sampling both branches arrive at a common $0.4\arcsecpx$ grid. The two streams are then summed with learned identity embeddings that let the network tell Rubin features from \textit{Euclid} ones, and passed to a transformer bottleneck \citep[][]{transformers}. Through self-attention, every location in the fused map can exchange information with every other, and it is here that cross-instrument and cross-band relationships are combined. Per-instrument decoders expand the bottleneck features back to native resolution to reconstruct the withheld band, with the target band specified to the decoder so that a single model can reconstruct any of the bands. The complete model contains roughly nine million parameters; layer-by-layer specifications are given in Figure~\ref{fig:arch}.

Within each branch the per-band feature maps must be combined before the streams are fused. A natural default is to pool them into a single map, but doing so attenuates each band's contribution to the learning signal: averaging six Rubin bands, for example, divides each band's gradient by six, so the encoder is discouraged from forming sharp per-band features. The model instead concatenates the per-band features and projects them, preserving each band's identity through the encoder at full strength. This keeps wavelength-dependent information, such as how a galaxy's morphology and effective PSF change from band to band, available to the representation, where the photometry and deblending heads later draw on it. When a band is withheld as the reconstruction target its input slot is zero-filled, and the projection learns to ignore the empty slot, which also lets the model operate when some bands are absent.

The reconstruction loss is computed in noise-normalized units and weighted toward pixels carrying source signal, so the model is judged on how well it reproduces astronomical structure rather than blank sky. Without this weighting the loss would be dominated by the empty background that fills most of every image, and the model could drive it down while reproducing sources poorly. A robust per-pixel loss is used, with additional weight on the bright cores of sources.

\subsection{From latent alignment to pixel reconstruction}

The masked-band objective was not the first design we tried, and the reconstruction approach was reached only after several alternatives failed to deliver the spatial precision the downstream tasks require. We summarize that history here because the negative results explain why reconstruction, rather than the more common alternatives, is the right objective for this problem.

The natural approach to relating two instruments is \emph{representation alignment}: encode a Rubin image and the matching \textit{Euclid} image each into a compact feature vector, and train the network so that the two vectors agree where they view the same sky. This is the principle behind contrastive learning \citep{contrastive} and Joint-Embedding Predictive Architectures (JEPA; \citealt{JEPA}), both widely used in computer vision. We designed and tested several cross-instrument variants in this family: patch-level contrastive matching, an object-centric model that attempts to discover individual sources, and JEPA architectures operating on the full image at native resolution with increasingly strict constraints forcing features at corresponding positions to match.

These approaches shared a common limitation that is structural rather than a matter of tuning. Alignment objectives reward features that are \emph{similar} but do not require the network to encode \emph{where} sources are located: two image regions can map to nearly identical feature vectors while differing in the sub-pixel positions of the sources they contain, and sub-pixel position is the quantity that astrometry and detection depend on. A direct control made this explicit: a simple supervised convolutional network, trained directly on the cross-instrument alignment task, outperformed the best self-supervised JEPA features at substantially lower cost.

Pixel reconstruction removes this failure mode by construction. To predict a withheld band at the correct positions, brightness, and morphology, the representation has no choice but to preserve the spatial layout of every source, a constraint that similarity-based objectives never impose. This is why the masked-band objective, rather than alignment, underlies the model described above.

\subsection{Implementation}\label{sec:code}

The foundation model was trained on two NVIDIA L40S GPUs using \texttt{PyTorch} with bfloat16 mixed-precision arithmetic and distributed data parallelism. Optimization used AdamW with gradient clipping at unit norm, a five-epoch linear warmup followed by cosine decay, and an effective batch size of four, for 50 epochs; the checkpoint with the lowest validation loss was retained.

Pre-training completed in roughly 1.25 wall-clock hours ($\sim2.5$ GPU-hours). Once trained, the encoder is frozen and run once over the full dataset to extract and cache per-tile features; each downstream head is then a small network trained on these cached features rather than on the full imaging. The feature-extraction pass is the main per-dataset cost, but like the pre-training it is incurred only once and shared across all heads. Training was monitored with the Weights \& Biases platform.

The reconstruction loss is evaluated in noise-normalized units, with each pixel weighted by a signal-and-gradient information map so that source pixels dominate over empty sky. The per-pixel term is a Charbonnier penalty, supplemented by an additional squared-error term on the highest-information ``core'' pixels of sources. The exact functional form and all coefficients are given in the public code.
The complete implementation, including the foundation model, the reconstruction loss, the downstream heads, and the scripts that reproduce the analyses and figures in this paper, is publicly available.\footnote{\url{https://github.com/xoubish/JAISP}}

\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{figures/fig3_reconstruction.png}
    \caption{Masked-band reconstruction on three held-out ECDFS tiles
    (validation split, never seen during training), validating the
    self-supervised pre-training objective. In each row a single band is withheld and reconstructed from the other nine: Rubin $r$ (top), Euclid VIS (middle), and Euclid NISP $H$ (bottom). Columns show the true band (left), the reconstruction (center), and the residual truth $-$ reconstruction (right); panels are $48''$ on a side. The residual uses a diverging scale whose limits are the brightest source pixel of that row
    ($\pm$pk), so full color saturation would require an error as large as the peak source flux; the near-white residuals therefore indicate that reconstruction errors are a small fraction of the signal, concentrated only at the cores of the brightest point sources and in the faint extended wings of large galaxies. The annotated $r$ is the Pearson correlation between truth and reconstruction over the brightest 10\% of pixels; averaged across the 39 held-out tiles the mean per-band correlation is $0.99$ ($r$), $0.77$ (VIS), and $0.90$ ($H$). The foundation reconstructs unseen sky faithfully across both instruments and the full optical-to-near-infrared range, confirming it is
    well trained and ready to drive the downstream heads.}
    \label{fig:foundation_reconstruction}
\end{figure}


\subsection{Validation of the learned representation}\label{sec:validation}

Before attaching any task-specific head, we verify that the frozen representation has captured the cross-instrument structure it was trained to learn. We do this in three ways: by checking that held-out bands can be reconstructed faithfully, that the per-band information is recoverable from the frozen features a head would use, and that the model routes information between bands in a physically sensible way.

\textit{Reconstruction fidelity.} Figure~\ref{fig:foundation_reconstruction} shows masked-band reconstruction on validation tiles never seen during training. In each case a single band is withheld and predicted from the other nine, sampled across the full input range: a Rubin optical band, the high-resolution \textit{Euclid} VIS channel, and a \textit{Euclid} NISP near-infrared band are shown. The reconstructions reproduce the held-out sky faithfully, with residuals confined to the cores of the brightest point sources and the faint extended wings of large galaxies, and with high truth-to-reconstruction correlation across both instruments and the entire optical-to-near-infrared range. That a band from one instrument can be predicted from bands of the other confirms the model has learned genuine cross-instrument correspondence, not merely within-instrument copying.

\begin{figure*}
    \centering
    \includegraphics[width=\textwidth]{figures/fig4_probe.png}
    \caption{Two views of the frozen foundation representation. \emph{Left
    (decodability):} a probe predicts each band's per-source brightness ($\log S/N$) from the frozen bottleneck features at detected source positions (6-fold spatial hold-out, $\sim$770 sources; features standardized, ridge penalty cross-validated, and per band restricted to sources with signal in that band). Open markers are a linear (ridge) probe, filled markers a
    nonlinear (MLP) probe, the connecting line is the nonlinear gain, and the gray bar is the fold scatter. All ten bands are linearly decodable ($R^2 = 0.7$--$0.95$); VIS reads back lowest only because sources are selected on VIS $S/N$, truncating its range. \emph{Right (information routing):} input-gradient attribution---mask a target band (row) and backpropagate the reconstruction loss to each source band's pixels (row-normalized; the diagonal is self-attribution, shown gray). VIS is omitted because, when present, it absorbs $\sim$100\% of the attribution (the universal spatial scaffold); shown is the routing among the remaining nine bands. $n_{\rm eff} = 2^{H}$ ($H$ the Shannon entropy of each row) is the effective
    number of source bands: Rubin bands draw on many sources
    ($n_{\rm eff}\approx4$--$7$: spectral neighbors, with a $g/r$ hub, plus NISP), whereas the NISP bands form a near-closed block ($n_{\rm eff}\approx3$:
    $Y/J/H$ mutually, with only $\sim$10\% of their attribution reaching Rubin). The right panel is a VIS-absent counterfactual that isolates this secondary routing.}
    \label{fig:foundation_probe}
\end{figure*}


\textit{Decodability.} Faithful reconstruction shows the information is present in the model, but not that it is accessible in the frozen features a downstream head consumes. We test this with a linear probe (left panel of Figure~\ref{fig:foundation_probe}): a ridge regression trained to predict each band's per-source brightness from the frozen features at detected source positions, evaluated under a spatial hold-out so the probe is always tested on sources outside its training set, with features standardized per fold and the regression restricted per band to sources actually detected in that band. All ten bands are recovered with high accuracy ($R^2 \approx 0.7$--$0.92$), and a nonlinear probe adds little, showing the per-band photometric information is not only present but \emph{linearly} available in the representation. We note that source brightness is an easy quantity to decode, so this is a check on representational richness rather than a strong claim about deep cross-instrument content; the more discriminating question is addressed next.

\textit{Information routing.} The right panel of Figure~\ref{fig:foundation_probe} asks which bands the model actually draws on when reconstructing a given band. We measure this by input-gradient attribution: for each withheld target band, we backpropagate the reconstruction loss to the input pixels of every source band and sum the resulting sensitivities, giving the fraction of the reconstruction that each source band drives. The first result is stark. When \textit{Euclid} VIS is available it absorbs essentially all of the attribution: as the sharpest and deepest channel, it serves as a universal spatial scaffold on which every other band is reconstructed. Because this single-band dominance would otherwise mask all finer structure, the panel shown is a counterfactual with VIS removed, exposing how the remaining nine bands route information among themselves. To summarize each target band's reliance on others compactly, we report the effective number of contributing source bands, $n_{\rm eff} = 2^{H}$, where $H$ is the Shannon entropy of that band's normalized attribution distribution; $n_{\rm eff}$ is a well-defined, assumption-free measure that equals one when a band is reconstructed from a single other band and grows as the reconstruction spreads its reliance across many. The Rubin optical bands have $n_{\rm eff} \approx 4$--$7$, drawing on many spectral neighbors with the adjacent bands contributing most, whereas the three NISP near-infrared bands form a near-closed block ($n_{\rm eff} \approx 2$--$3$) that relies chiefly on one another and barely on Rubin. This mirrors the structure of real galaxy spectral energy distributions, where neighboring bands are most predictive of one another, and indicates that the representation has organized the bands by genuine physical relationships rather than arbitrary correlations.
Together these checks establish that the frozen representation is faithful, that its per-band information is linearly accessible, and that it routes information between bands in a physically coherent way. It is therefore a sound basis for the task-specific heads developed in the following sections.


\section{Detection}\label{sec:detection}
\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig5_detection.png}
\caption{\textbf{Detection on a Euclid VIS frame: the foundation head versus classical and catalogue references.} A single $1.8'$ VIS tile (\texttt{tile\_x01792\_y00000}, $0.1''/\mathrm{px}$, held out from the detection training set in a spatially-disjoint patch split), with four source lists overlaid. \emph{Cyan circles:}
the classical VIS source list (\textsc{SEP} source extraction at $3\sigma$, with bright-star and duplicate cleaning). \emph{Magenta crosses:} the foundation CenterNet head operating on the frozen, fused 10-band bottleneck.
\emph{Orange pluses:} the StemCenterNet variant on native per-instrument stems. \emph{Red squares:} the clean Euclid Q1 MER catalogue (VIS-detected, non-spurious). The white bar is $15''$.}
\label{fig:detection_overlay}
\end{figure*}


\begin{table}[t]
\centering
\caption{Detection-head comparison on the frozen foundation. Source-recycling injection on
the same 108 held-out tiles (a spatially-disjoint patch) at threshold $0.30$; $d_{50}$ is the 50\% point-source depth in
VIS magnitudes. Only the fused bottleneck converts the other nine bands into added depth.
NISP-only injection reaches the $50\%$ level for neither head.}
\label{tab:detection_heads}
\begin{tabular}{lcc}
\toprule
                       & CenterNet & StemCenterNet \\
                       & (fused)   & (stems)       \\
\midrule
$d_{50}$, all 10 bands & $25.0$  & $25.1$  \\
$d_{50}$, VIS only     & $24.6$  & $25.1$  \\
multi-band gain        & $+0.45$ & $+0.03$ \\
\bottomrule
\end{tabular}
\end{table}

Source detection is the first downstream test of the foundation representation, and it asks a specific question: does the frozen latent carry multi-band source evidence beyond what a single sharp band already provides, and can a lightweight head built on it match classical detection and then go further? Detection on \textit{Euclid} VIS alone is sharp and reliable, but it cannot use color, low-surface-brightness structure, or the Rubin and NISP channels in a learned way. We compare three source lists that differ only in what information they see: a classical VIS source-extraction list (\textsc{SEP}), which serves as the baseline and as the source of training labels, and two variants of a learned detection head built on the frozen foundation features. Comparing them isolates whether the representation has learned genuine multi-band source evidence, and whether that evidence is best expressed in the fused latent or in the higher-resolution per-band features.

Both variants share the same output design and differ only in the features they read. Following CenterNet \citep[][]{centernet}, each predicts a dense source-evidence map over the tile, in which a true source is represented during training by a small Gaussian peak at its position rather than by a single hard pixel, so that nearby pixels receive a graded target suited to sub-pixel positions. A focal loss handles the extreme imbalance between the rare source pixels and the dominant empty sky. Alongside the evidence map each predicts a sub-pixel offset and a brightness proxy at every location. At inference the procedure is essentially learned peak-finding: local maxima above a confidence threshold are kept, and the offset is read off to refine each position. This is the same operation as classical peak-finding, but performed on a learned ten-band source-evidence image rather than on a single band.

The two variants differ in where they read that evidence. The fused-bottleneck variant (CenterNet) operates on the frozen bottleneck, where the foundation model has already mixed all ten bands, and so tests whether the representation has learned multi-band source evidence. The native-stem variant (StemCenterNet) operates instead on the per-band stem features at their original resolution, projected onto a common grid, and so tests whether finer spatial detail helps localization beyond what the coarser bottleneck retains. Each adds only a few million trainable parameters on top of the frozen encoder.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig6_detection_performance.png}
\caption{Detection performance of the foundation CenterNet head, which reads the frozen 10-band fused bottleneck, evaluated on 108 held-out tiles (a spatially-disjoint patch, never seen in training). \emph{Top:} completeness versus VIS magnitude. Solid curves are catalog-independent source-recycling injection with the source placed in all 10 bands (blue), VIS only (orange), or NISP $Y\!/\!J\!/\!H$ only (red); the shaded band between all-10 and VIS-only is the $\approx0.45$\,mag gained from multi-band fusion, giving a 50\% point-source depth of VIS\,$\approx25.0$. The dashed grey curve is recovery of the real clean Euclid Q1 MER catalog, and the grey histogram (arbitrary scale) shows the MER VIS-magnitude distribution. MER recovery stays high ($\sim$85--97\%) across the range the catalog reaches. \emph{Bottom left:} the same MER-catalog recovery (full 10-band detector at threshold $0.30$) binned by \emph{measured} VIS aperture S/N rather than magnitude, independent of any photometric zeropoint. \emph{Bottom right:} completeness (VIS\,$<24.5$) and purity as the detection threshold is varied, with the $0.30$ working point marked; purity is measured against the full MER catalog and is a conservative floor, since some unmatched detections are real sources absent from the VIS-selected MER catalogue.}
\label{fig:detection_performance}
\end{figure*}


We also tried a Detection Transformer \citep[DETR;][]{DETR}, which represents objects with a fixed set of learned ``query'' slots assigned to sources through a matching step. Its set-prediction machinery is built for the complexity of natural scenes and is data-hungry, and on our limited training set it converged slowly and unstably. A per-pixel design fits the problem better: every pixel receives a direct training signal, and detection reduces to finding peaks in a learned source-evidence image, much as sources are found classically. Blending, the main concern for such peak-based approaches, is also less severe here, since the fused representation brings the sharp \textit{Euclid} VIS scaffold to bear alongside the ground-based bands.

A natural source of labels would be the Euclid Q1 MER catalog, but we deliberately did not train on it. Detection should not require heavy curated supervision, so we keep the labels lightweight and internal, generating them directly from the VIS imaging with standard source extraction \citep[\textsc{SEP};][]{SEP} at $3\sigma$, followed by bright-star and duplicate cleaning. We compared this against a multi-scale peak-finder; \textsc{SEP} produced the cleaner reference---markedly higher agreement with the MER catalogue at matched purity---so we adopt it as the primary recipe. The head is trained on these labels.

We use the MER catalog instead as an external cross-check. Training the head on MER labels rather than our internal ones gives essentially the same detector---the same injection depth, with only a marginal change in catalogue-match agreement (itself circular, since a MER-trained model is scored against MER). This is consistent with MER's role here: the Q1 MER catalog is VIS-selected rather than a complete truth catalog, so it neither adds information beyond our VIS-based labels nor rewards the deeper, multi-band sources the head recovers that are faint in VIS but supported by the other bands.

Figure~\ref{fig:detection_overlay} overlays the four source lists on a single held-out \textit{Euclid} VIS tile. The overall agreement is good: on the great majority of sources the four lists coincide. Two differences are visible on close inspection. First, the classical \textsc{SEP} list tends to over-fragment extended galaxies, placing several detections across the spiral arms or star-forming clumps of a single resolved system, whereas the learned heads more often return one detection per object. Second, among the learned heads the fused CenterNet variant is visibly the strongest: it recovers more faint sources than either the classical list or the native-stem variant, including objects that are faint in VIS but supported by the other bands. Whether the detector's added faint sources are real rather than noise is what the injection test below quantifies, by showing that real faint multi-band sources are in fact recoverable; the overlay shows the qualitative pattern, and the quantitative comparison follows.

We attach the detection head to the frozen foundation at the fused, multi-instrument bottleneck (the $0.4''/\mathrm{px}$ latent of Fig.~\ref{fig:arch}) and train only the head. Depth is measured by injection, since catalog matching cannot probe past the reference catalog's own incompleteness at the faint end: using source recycling, we take real, isolated, compact sources of known VIS magnitude, extract their full ten-band cutouts (real PSF and SED), rescale them to a fainter target magnitude, inject them at empty positions, and re-run the frozen detector; recovery as a function of injected magnitude gives a catalog-independent completeness curve (Fig.~\ref{fig:detection_performance}, top). The fused head reaches a $50\%$ point-source depth of VIS\,$\approx25.0$, about $0.4$\,mag deeper than the same detector restricted to VIS alone ($24.6$) -- a multi-band gain that pushes it past the effective turnover of the VIS-selected MER catalog. Running the identical benchmark on the native-stem head (StemCenterNet) confirms that this gain is a property of the fused representation, not of the head: it reaches the same VIS-driven depth but gains essentially nothing from the other nine bands ($+0.03$\,mag vs $+0.45$\,mag; Table~\ref{tab:detection_heads}). We therefore adopt the fused CenterNet configuration for all subsequent results.

To localize where this sensitivity comes from, we repeat the injection placing the source in only one instrument subset at a time. Injecting into the NISP bands alone yields only partial recovery, peaking below $40\%$ at the bright end and never reaching the $50\%$ level; injecting into the Rubin bands alone yields essentially no detections. The head is \textit{Euclid}/VIS-anchored---operating at VIS resolution and trained on VIS-detected labels---so the other bands add depth in fusion but cannot drive detections on their own.

Against the clean MER catalog, the detector recovers $\sim$85--97\% of listed sources across the magnitude range MER reaches (Fig.~\ref{fig:detection_performance}, top, dashed curve), with
the same recovery binned by measured VIS aperture S/N shown in the lower-left panel so that it does not depend on a photometric zeropoint. Purity, measured against the full MER catalog, rises with the detection threshold as completeness falls (lower-right panel); we adopt a working point of $0.30$. Because MER Q1 is VIS-selected rather than a complete truth catalog, these numbers describe agreement with a VIS reference, not completeness or purity against truth: the unmatched detections include real multi-band sources absent from MER---they carry Rubin/NISP aperture flux well above random sky positions---so the quoted purity is a conservative lower bound.

In summary, the head begins from classical VIS detection and goes beyond it: it is deeper, uses all ten bands, and returns one detection per object where classical source extraction fragments extended systems. Its main limitation lies at the other end of the size distribution -- large, low-concentration galaxies whose light is spread over many pixels are occasionally missed, because the $0.4''/\mathrm{px}$ fused bottleneck on which the head operates cannot concentrate
their flux into a sharp peak. A finer bottleneck would address this and is the natural next step.


\section{Astrometry}\label{sec:astrometry}


\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/fig7_astrometry_agreement.png}
    \caption{Raw astrometric agreement between Rubin/NISP and \textit{Euclid} VIS
    \emph{before any correction}, from classical Gaussian-centroid positions of matched sources pooled over the ECDFS field; plotted offsets are (Rubin/NISP $-$ VIS). \emph{Left:} two-dimensional offset clouds, each contour enclosing $50\%$ of that band's sources. Solid contours are all detections; dashed contours are bright sources ($\mathrm{S/N}\geq30$). Although both surveys are independently registered to Gaia, the bright clouds do \emph{not} close on the origin: they sit at a small, band-dependent systematic offset of a few mas. Notably, the NISP bands ($Y\!/\!J\!/\!H$) sit at a \emph{larger} offset ($\sim$7\,mas) than the Rubin bands ($\sim$3\,mas)
    despite sharing VIS's pixel grid, a registration residual of the
    $0.3\arcsec\!\rightarrow\!0.1\arcsec$ resampling of NISP onto the MER grid. The legend lists the bright\,/\,all median offset per band; the $u$ and $y$ bands are given as numbers only, their clouds lying off the plotted scale. \emph{Right:} median radial offset versus signal-to-noise for the six well-measured bands, following the expected $\mathrm{FWHM}/\mathrm{SNR}$
    degradation; the dotted line marks the $\mathrm{S/N}=30$ bright cut. Bright sources already agree to $\sim$8--18\,mas but are both offset and sparse ($\sim$1--3\,arcmin$^{-2}$), whereas the faint-galaxy-dominated full population disagrees by $\sim$50--60\,mas. Measuring and removing that systematic offset (the concordance field) and shrinking the per-source scatter (the position head) are the two goals of this section.}
    \label{fig:astrom_agreement}
\end{figure*}

\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/fig8_head_correction.png}
    \caption{Effect of the per-object latent position head on cross-survey agreement, using the same offset measurement as Fig.~\ref{fig:astrom_agreement}
    (Rubin/NISP $-$ \textit{Euclid} VIS) over all 790 ECDFS tiles. Solid curves are astrometry head-corrected; dashed curves are the raw classical centroids. \emph{Left:} all-detection offset clouds (each contour encloses $50\%$ of that
    band's sources). The head collapses the raw $\sim$50\,mas clouds to $\sim$11--16\,mas and re-centres them on the origin, removing both the
    per-source scatter and the systematic offset; the legend gives the raw\,$\rightarrow$\,head median per band ($u$ and $y$ raw clouds lie off the plotted scale). \emph{Right:} median radial offset versus signal-to-noise. The raw offset rises to $\sim$70\,mas at the faint end (dashed), whereas the head-corrected offset (solid) is far flatter---the head brings faint sources down toward the bright-source floor, largely removing the $\mathrm{FWHM}/\mathrm{SNR}$ degradation, though a residual dependence remains.}
    \label{fig:head_correction}
\end{figure*}

\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/fig9_concordance_field.png}
    \caption{The cross-survey concordance field before and after astrometry head correction.  \emph{Top:} the
    NISP\,$J$ residual field over the ECDFS footprint (arrows: direction; color: $|$field$|$; smoothed to $150\arcsec$). Before the head measured with two solvers: the physics-informed
    network (PINN, left) and the hierarchical Gaussian process (HGP, center). They recover the same $\sim$5--7\,mas pattern. After the per-object correction head (right, \emph{same color scale}) the coherent field collapses to $\sim$1\,mas: the head absorbs it
    source-by-source. \emph{Bottom:} the HGP
    hierarchical decomposition of the pre-head field into a component common to all bands ($\sim$4\,mas), per-instrument group terms (Rubin, NISP), and small
    per-band residuals ($u$--$H$); the dominant common term shows most of the field is a single shared cross-survey systematic rather than per-band noise.}
    \label{fig:concordance_field}
\end{figure*}

Astrometry is the second downstream test of the foundation representation, and the question it asks is whether the joint learned representation can improve astrometry itself: both the precision of individual source positions and the alignment between the two surveys. Rubin and Euclid each arrive with a mature astrometric solution, independently registered to the Gaia reference frame \citep{gaia2016, gaia2023}, and this work does not attempt to improve either. But nothing in what follows relies on that prior registration. The per-source correction and the cross-survey alignment developed here are built entirely from the pixels: the representation learns where the two instruments see the same sources and uses that correspondence directly. In principle the same machinery would align two surveys that share no external reference frame at all; the Gaia registration matters only for tying the result to an absolute frame, which we do not address here. What the shared frame does provide in this case is a stringent starting point, since two independently Gaia-aligned surveys should already agree, and measuring how well they actually do is itself informative. Concretely, this section develops two products: a per-source position head that transfers the sharp VIS localization to the nine other bands, where the PSF is broader or the source fainter, and a measurement of the residual agreement between the two solutions, which we call the cross-survey concordance field. The two live at very different amplitudes, and separating them cleanly is the main work of this section.

Figure~\ref{fig:astrom_agreement} shows the starting point: the raw astrometric agreement between the surveys before any correction, from classical Gaussian-fit centroids of sources matched between each band and VIS, pooled over the training field. Bright sources (S/N $\geq 30$) already agree well, with per-band median radial offsets of ${\sim}8$--$18$\,mas, but their offset clouds do not close on the origin: each band sits at a small, coherent systematic offset of a few mas. Notably, the NISP bands sit at a larger offset (${\sim}7$\,mas) than the Rubin bands (${\sim}3$\,mas) despite being delivered on the same pixel grid as VIS; this is a registration residual of resampling the native $0.3''$ NISP frames onto the $0.1''$ MER grid, a preprocessing step inherited with the mosaics (Section~\ref{sec:data}). The full source population, dominated by faint galaxies, tells a different story: it disagrees at ${\sim}50$--$60$\,mas, and the right panel shows that this disagreement follows the expected photon-noise scaling of centroid precision with FWHM and S/N \citep{king1983}. The raw offset is therefore not one error but two, of different nature and very different amplitude.

This decomposition dictates the strategy. The coherent component is small, a few mas, and is carried by bright sources that are sparse on the sky (${\sim}1$--$3\,\mathrm{arcmin}^{-2}$); it is a smooth field to be mapped. The dominant component is per-source centering noise: it lives at the photon-noise floor set by each band's PSF and depth, it is uncorrelated between neighboring sources, and no global correction can remove it. It has to be corrected one source at a time, conditioned on the actual image content, which is precisely what a head on the foundation representation can do.

The position head is a small network (${\sim}0.7$M parameters) attached to the frozen encoder. For each detected source it reads two local windows: one from the fused bottleneck, whose $0.4''$\,px$^{-1}$ features carry the chromatic morphology of all ten bands, and one from the VIS stem features at their native $0.1''$\,px$^{-1}$ sampling, which provide the fine spatial reference. From these it predicts a per-source, per-band position offset together with its uncertainty. Training is self-supervised and requires no external catalog: VIS Gaussian-fit centroids serve as targets, the query position is perturbed by a controlled jitter, and the head learns to recover the centroid that the local features encode. The loss is a Rayleigh negative log-likelihood that includes a fixed label-noise floor of 5\,mas, reflecting the systematic floor of the underlying WCS solutions, so that the head is not rewarded for chasing noise in individual bright-source labels.

Figure~\ref{fig:head_correction} shows the effect. The head collapses the raw ${\sim}50$\,mas offset clouds to ${\sim}11$--$16$\,mas medians in every non-VIS band and re-centers them on the origin, removing the per-source scatter and the coherent band offsets together. The S/N dependence flattens correspondingly: faint sources are brought down toward the bright-source floor, largely removing the FWHM/S/N degradation, though a residual dependence remains. Because the head is trained against classical VIS centroids, agreement with those labels could in principle reflect emulation of the label convention rather than genuinely better positions; we test this directly by injecting sources of known position into all ten bands of held-out tiles. At a peak S/N of 5, head positions err against truth by ${\sim}19$\,mas, compared to ${\sim}47$\,mas for classical single-band centroids, close to the ${\sim}17$\,mas floor set by the VIS anchor itself. The mechanism is exactly the transfer the section set out to test: the head carries VIS's localization to the other bands, and it adds no precision beyond what VIS itself provides. \todo{add Ian's result on the dependence of centering accuracy on source axis ratio $a/b$, with figure}

With the per-source scatter under control, the small coherent component can be measured cleanly. Figure~\ref{fig:concordance_field} shows the concordance field, fit to the bright-source offsets with two solvers of deliberately different character: a physics-informed neural network (PINN; \citealt{PINN}) whose auxiliary losses impose curl-free structure, smoothness, and band consistency, and a hierarchical Gaussian process (HGP) that decomposes the field into a component common to all bands, per-instrument group terms, and per-band residuals, with posterior uncertainties. The point of using two solvers is that they can fail differently: the PINN constrains the field through physical priors, the HGP through hierarchical smoothness with calibrated posterior uncertainty, and structure that survives both is unlikely to be an artifact of either. At degree scales they recover the same ${\sim}5$--$7$\,mas pattern. Below that scale the comparison is no longer meaningful: refitting the PINN on identical inputs reproduces its own field only to ${\sim}5$\,mas, so sub-degree differences between the solvers are within the fit noise, and we do not interpret structure there. The HGP decomposition shows the field is dominated by its common component (${\sim}4$\,mas), shared across all ten bands: most of it is a single cross-survey systematic between the two Gaia-tied solutions rather than per-band noise. After the head correction the coherent field collapses to ${\sim}1$\,mas, absorbed source by source. We emphasize that the concordance field is a measurement, not a correction: applying the fitted field to held-out positions does not improve them, and its value is as a map of how well two independently calibrated surveys agree on the sky, at the few-mas level over degree scales.

\section{OOD}\label{sec:ood}
\begin{itemize}
    \item do results persist on out of distribution data (another field, EDF-S, no retraining)
    \item detection transfers nearly perfectly: completeness 89.6\% (OOD) vs 90.8\% (in-dist) -- figure 11
    \item astrometry: modest $\sim$5--15\% penalty (the scary $\sim$1.5$\times$ was a MAD-variance artifact, resolved with real variance) -- figure 12
    \item bright-star suppression reproduces (stronger, corr $-0.71$): origin is input normalization, not the field
    \item can be extended all sky
\end{itemize}

\section{Discussion}\label{sec:discussion}
\begin{itemize}
    \item DR1 better processing and deeper than Q1, also Native rather than processed mosaics
    \item better PSF, better centering
    \item classical to inform model training, but faster inference once learned
    \item label noise limits in-distribution metrics
    \item next paper
\end{itemize}

\begin{acknowledgments}
\todo{acknowledgments, facilities, software}
\end{acknowledgments}

\facilities{Rubin Observatory, Euclid}
\software{Astropy \citep{astropy2022}, NumPy, SciPy, PyTorch, SEP}

\bibliographystyle{aasjournalv7}
\bibliography{refs}

\end{document}
