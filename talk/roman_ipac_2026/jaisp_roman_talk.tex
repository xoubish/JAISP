% ====================================================================
% JAISP talk -- Roman Science Conference, Caltech/IPAC, July 13-16 2026
% 15-minute talk (~12 min + questions).
% Build:  latexmk -pdf jaisp_roman_talk.tex
% Figures are pulled directly from ../../paper/figures/
% ====================================================================
\documentclass[aspectratio=169,10pt]{beamer}

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\graphicspath{{figures/}{../../paper/figures/}}

% --- clean look ---
\usetheme{default}
\usecolortheme{seagull}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{itemize items}[circle]
\setbeamertemplate{itemize subitem}[circle]

\definecolor{jaispblue}{RGB}{20,60,110}
\definecolor{jaispteal}{RGB}{0,120,130}
\definecolor{jaispaccent}{RGB}{190,70,25}
\setbeamercolor{frametitle}{fg=white,bg=jaispblue}
\setbeamercolor{title}{fg=jaispblue}
\setbeamercolor{structure}{fg=jaispblue}
\setbeamercolor{alerted text}{fg=jaispaccent}
\setbeamerfont{frametitle}{series=\bfseries,size=\large}
\setbeamertemplate{frametitle}{%
  \vspace*{0.25em}%
  \begin{beamercolorbox}[wd=\paperwidth,sep=0.5em]{frametitle}%
    \insertframetitle\hfill{\normalfont\small\insertframenumber/\inserttotalframenumber}%
  \end{beamercolorbox}}

\newcommand{\jaisp}{\textsc{Jaisp}}
\newcommand{\mas}{\,\mathrm{mas}}
\newcommand{\srcnote}[1]{{\tiny\textcolor{gray}{#1}}}

\title{{\Huge\jaisp}\\[0.5em]
  \Large A Joint Rubin--\textit{Euclid} Foundation Model for Detection and Astrometry\\[0.3em]
  \large --- and a path to \textit{Roman} ---}
\author{Shoubaneh Hemmati\\[0.3em]
  {\small with G.\ Helou, Y.-H.\ Lin, R.\ Akeson, C.\,D.\ Dowell, A.\ Faisst, T.\ Greene, D.\ Shupe}}
\institute{Caltech/IPAC, NASA JPL}
\date{Roman Science Conference $\cdot$ July 2026}

\begin{document}

% ------------------------------------------------------------ 1
\begin{frame}[plain]
  \titlepage
  \vfill
  \centering
  \srcnote{Hemmati et al.\ 2026, in prep.\ \quad$\cdot$\quad code: github.com/xoubish/JAISP}
\end{frame}

% ------------------------------------------------------------ 2
\begin{frame}{Why combine surveys at the pixel level?}
  \begin{itemize}\setlength\itemsep{0.7em}
    \item Cosmology is now limited by \alert{systematics, not statistics}: whether the
      tensions between cosmological probes are real physics or measurement error, settling
      them requires control of systematics at a level no survey has yet demonstrated.
    \item Rubin/LSST, \textit{Euclid}, and \textit{Roman} will observe overlapping sky from
      ${\sim}0.3$ to $2\,\mu$m at sub-arcsecond resolution --- combined at the pixel level,
      they can \alert{cross-check one another}. But that means handling \alert{different
      resolutions, PSFs, depths, and noise} at once, which no single-survey pipeline was
      built to do.
    \item AI will not replace decades of classical pipeline effort --- certainly not all at
      once; reaching that maturity takes \alert{comparable, problem-specific work}, and
      classical methods \alert{inform the learning} along the way (here: they provide our
      training labels and every benchmark we test against).
  \end{itemize}
\end{frame}

% ------------------------------------------------------------ 3
\begin{frame}{Data: the earliest public release of each survey}
  \centering
  \includegraphics[width=0.86\textwidth]{fig1_data.png}
  \begin{itemize}\small
    \item \textbf{Rubin DP1}: commissioning camera, $ugrizy$, $0.2''$/px, ${\sim}1''$ seeing.
    \item \textbf{\textit{Euclid} Q1}: VIS and near-infrared $YJH$, $0.1''$/px.
    \item \textbf{Training field (ECDFS)}: 790 overlapping ten-band tiles over
      ${\sim}0.2\,\mathrm{deg}^2$.
    \item \textbf{Out-of-distribution field (EDF-S)}: \alert{set aside untouched} --- the model
      never sees it --- to test at the end whether everything transfers to new sky.
  \end{itemize}
\end{frame}

% ------------------------------------------------------------ 4
\begin{frame}{Train one model once --- reuse it for every measurement}
  \begin{columns}[T]
    \begin{column}{0.58\textwidth}
      \textbf{Self-supervised pretraining} (masked-band reconstruction):
      \begin{itemize}
        \item mask one of the ten bands; reconstruct it from the other nine
        \item \alert{no labels, no catalogs, no simulations}
      \end{itemize}
      \vspace{0.5em}
      \textbf{Freeze the encoder, reuse the features:}
      \begin{itemize}
        \item each task is a \alert{lightweight head} on the same cached features
        \item detection \& astrometry today; photometry, shapes, redshifts, \ldots\ in preparation
      \end{itemize}
    \end{column}
    \begin{column}{0.38\textwidth}
      \begin{beamercolorbox}[sep=0.8em,rounded=true]{block body}
        \textbf{Cost structure}\\[0.4em]
        Pretraining: \alert{${\sim}2.5$ GPU-hours}\\[0.2em]
        Model size: ${\sim}9$M parameters\\[0.2em]
        Features: computed once per dataset\\[0.2em]
        Each new task: a lightweight head\\[0.4em]
        {\small $\Rightarrow$ marginal cost per task $\to$ small; per field $\to$ zero (as we will see)}
      \end{beamercolorbox}
    \end{column}
  \end{columns}
\end{frame}

% ------------------------------------------------------------ 5
\begin{frame}{Architecture: each instrument at its native resolution}
  \centering
  \includegraphics[width=0.80\textwidth]{fig2_architecture.png}
  \begin{itemize}\small
    \item Rubin stays at $0.2''$/px, \textit{Euclid} at $0.1''$/px --- \alert{no resampling onto a
      shared grid}, so neither instrument's spatial information is thrown away.
    \item The branches merge on a common grid, where all ten bands exchange information.
  \end{itemize}
\end{frame}

% ------------------------------------------------------------ 5b
\begin{frame}{We didn't start here: ten iterations to this design}
  \small
  \begin{tabular}{@{}p{0.09\textwidth}p{0.60\textwidth}l@{}}
    \toprule
    v1--v2 & \textbf{Contrastive patch matching} --- make matching Rubin/\textit{Euclid} cutouts
             look similar in feature space & \textcolor{jaispaccent}{failed: sky-dominated} \\[0.35em]
    v3     & \textbf{Object-centric matching} --- discover individual sources, pair them across
             instruments & \textcolor{jaispaccent}{abandoned: no precision} \\[0.35em]
    v4--v5 & \textbf{Feature matching on full tiles (JEPA)} --- make the two instruments'
             features agree at corresponding positions & \textcolor{jaispaccent}{beaten by a simple CNN} \\[0.35em]
    v6     & \textbf{The turn: predict actual pixels} --- hide one band, reconstruct its image;
             no shortcut that discards position & \alert{\textbf{paradigm shift}} \\[0.35em]
    v7--v8 & \textbf{Mixed resolution} --- one branch per instrument at its native pixel scale,
             merged at a common physical scale & \textcolor{jaispteal}{works} \\[0.35em]
    v9--v10 & \textbf{Refinements} --- every band gets an equal vote in training; the loss
             focuses on real sources & \textcolor{jaispteal}{\textbf{production}} \\
    \bottomrule
  \end{tabular}
  \vfill
  \centering
  \emph{Features that look similar aren't features that know \textbf{where} ---
    contrastive, JEPA, masked autoencoding: we walked the road, and pixels won.}
\end{frame}

% ------------------------------------------------------------ 6
\begin{frame}{Did it learn cross-instrument structure?}
  \begin{columns}[T]
    \begin{column}{0.46\textwidth}
      \centering
      \includegraphics[height=0.72\textheight]{fig3_reconstruction.png}
    \end{column}
    \begin{column}{0.52\textwidth}
      \vspace{1.5em}
      \begin{itemize}\setlength\itemsep{0.8em}
        \item Held-out tiles: withhold one band, predict it from the other nine.
        \item The loss is \alert{noise-normalized (per-pixel variance) and weighted toward
          source pixels} --- the model is graded on astronomical structure, not on
          reproducing blank sky.
        \item Every band is reconstructed faithfully (three examples shown) ---
          residuals confined to bright cores and faint galaxy wings.
        \item A band from \alert{one instrument predicted from bands of the other}:
          genuine cross-instrument correspondence, not within-instrument copying.
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

% ------------------------------------------------------------ 7
\begin{frame}{...and is it organized physically?}
  \centering
  \includegraphics[width=0.88\textwidth]{fig4_probe.png}
  \begin{itemize}\small
    \item \textbf{Left:} a simple linear fit reads each band's brightness straight back out of
      the features ($R^2 \approx 0.7$--$0.95$) --- the information is there and easy to reach.
    \item \textbf{Right:} the model leans on bands the way real galaxy colors would predict:
      VIS is the sharp scaffold everything is built on; Rubin bands draw on their spectral
      neighbors; the three near-infrared bands mostly predict one another.
  \end{itemize}
\end{frame}

% ------------------------------------------------------------ 8
\begin{frame}{Downstream task 1 --- Detection}
  \begin{columns}[T]
    \begin{column}{0.50\textwidth}
      \centering
      \includegraphics[width=\textwidth]{fig_detection_heads.png}
    \end{column}
    \begin{column}{0.48\textwidth}
      \centering
      \includegraphics[width=0.66\textwidth]{fig5_detection.png}
      \begin{itemize}\footnotesize
        \item Same head design, two read-out points: \alert{fused ten-band features}
          vs.\ native per-band stems.
        \item Matches the \textit{Euclid} Q1 MER catalog on held-out sky;
          \alert{one detection per galaxy} where classical extraction fragments
          extended systems.
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

% ------------------------------------------------------------ 9
\begin{frame}{Detection: ten bands buy real depth}
  \begin{columns}[T]
    \begin{column}{0.60\textwidth}
      \centering
      \includegraphics[width=\textwidth]{fig6_detection_performance.png}
    \end{column}
    \begin{column}{0.40\textwidth}
      \vspace{1.5em}
      \begin{itemize}\small\setlength\itemsep{1.0em}
        \item Against the \textit{Euclid} Q1 MER catalog on held-out sky:
          \alert{93\% completeness, 95\% purity}.
        \item Injection test --- real sources, dimmed to a chosen magnitude, hidden in the
          images: ten-band detection is \alert{$+0.45$ mag deeper} than the same detector
          on VIS alone.
        \item On the deeper \textit{Euclid} DR1 imaging, the same injection test reaches
          \alert{VIS $\approx 26.0$}.
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

% ------------------------------------------------------------ 10
\begin{frame}{Downstream task 2 --- Astrometry: carrying VIS precision to every band}
  \centering
  \includegraphics[width=0.76\textwidth]{fig8_head_correction.png}
  \begin{itemize}\small
    \item The same sources measured in both surveys disagree by ${\sim}50\mas$ --- mostly
      photon noise in centroiding faint galaxies. A small correction network reads the model's
      features at each source and shrinks this to \alert{$14$--$17\mas$ in every band}.
    \item Ground-truth check: inject sources at \alert{known positions} and re-measure.
      At $\mathrm{S/N}=5$ the head recovers them to \alert{$19\mas$}; classical centroiding
      scatters by ${\sim}50\mas$. That is within ${\sim}2\mas$ of the VIS anchor itself ---
      \alert{VIS-quality positions in every band}.
  \end{itemize}
\end{frame}

% ------------------------------------------------------------ 11
\begin{frame}{The concordance field: how well do two Gaia-tied surveys actually agree?}
  \centering
  \includegraphics[height=0.60\textheight]{fig10_concordance_field.png}
  \begin{itemize}\small
    \item Beneath the per-source scatter sits a coherent \alert{$9$--$10\mas$ pattern} between
      the two surveys' astrometric solutions --- even though both are tied to Gaia; ${\sim}7\mas$
      is shared by \alert{all ten bands}. Two entirely different fitting methods recover the same
      pattern, and the per-source correction absorbs it (${\to}1.5\mas$).
    \item A few-mas map of \alert{how well two independently calibrated surveys actually
      agree} --- quality assurance no single pipeline can produce.
  \end{itemize}
\end{frame}

% ------------------------------------------------------------ 12
\begin{frame}{Does any of this transfer? A second deep field, nothing retrained}
  \centering
  \includegraphics[width=0.88\textwidth]{fig11_ood.png}
  \begin{itemize}\small
    \item The locked model and its measurement networks, applied to EDF-S: \alert{nothing retrained}.
    \item Detection: $93.4\%/93.1\%$ completeness/purity vs.\ $93.3\%/94.5\%$ on the training
      field. Positions land on the same ${\sim}12$--$18\mas$ floor, within $1$--$3\mas$.
    \item Scaling to more area is a matter of \alert{applying the stack, not rebuilding it}.
  \end{itemize}
\end{frame}

% ------------------------------------------------------------ 13
\begin{frame}{\textit{Roman}: the natural third stream}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \textbf{Why the model is ready:}
      \begin{itemize}\small\setlength\itemsep{0.4em}
        \item one encoder branch per instrument \emph{is} the design ---
          adding \textit{Roman} means \alert{adding a third branch}, not redesigning the model
        \item training needs only \alert{overlapping public images} --- no labels, no
          simulations --- and hours of GPU time, once WFI imaging overlaps Rubin/\textit{Euclid} sky
      \end{itemize}
      \vspace{0.4em}
      \textbf{What \textit{Roman} adds:}
      \begin{itemize}\small\setlength\itemsep{0.4em}
        \item \alert{sharp, deep near-infrared imaging at $0.11''$/px} --- the infrared
          counterpart of the sharp VIS reference, where today we must rely on resampled NISP
        \item a wide survey of thousands of deg$^2$ \alert{inside the LSST footprint},
          plus deep time-domain fields
      \end{itemize}
    \end{column}
    \begin{column}{0.46\textwidth}
      \textbf{What \textit{Roman} gains:}
      \begin{itemize}\small\setlength\itemsep{0.4em}
        \item detection that uses all surveys at once --- for Rubin$+$\textit{Euclid}
          that was worth $+0.45$ mag
        \item \textit{Roman}-quality positions carried into the ground-based bands
        \item \alert{three-way agreement maps}: Rubin--\textit{Euclid}--\textit{Roman}
          astrometry compared at the few-mas level --- quality assurance no
          single pipeline can produce
        \item cross-instrument alignment solved \emph{inside} the model instead of inherited
          from preprocessing (the ${\sim}3\mas$ NISP resampling error we found is exactly that)
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

% ------------------------------------------------------------ 14
\begin{frame}{Takeaways}
  \begin{itemize}\setlength\itemsep{0.9em}
    \item A small (${\sim}9$M-parameter) foundation model, trained \alert{without any labels}
      on public Rubin\,$+$\,\textit{Euclid} images in ${\sim}2.5$ GPU-hours, learns a shared
      ten-band representation precise enough to measure with.
    \item \textbf{Detection}: matches the \textit{Euclid} catalog (93\%/95\%), and ten-band fusion
      pushes \alert{$0.45$ mag deeper} than VIS alone.
    \item \textbf{Astrometry}: VIS-quality positions in every band --- ${\sim}50 \to 14$--$17\mas$,
      verified at $19\mas$ on hidden test sources --- and a few-mas \alert{map of the
      disagreement} between the two surveys' Gaia-tied solutions.
    \item The whole stack \alert{transfers unchanged} to a second deep field. All of this on the
      \emph{earliest} data both surveys have released --- a floor, not a ceiling.
    \item \textit{Roman} slots in as a \alert{third encoder stream}: sharp NIR anchor, three-way
      cross-survey calibration, and one representation reused across tasks, fields ---
      and instruments.
  \end{itemize}
  \vfill
  \centering
  \srcnote{Hemmati et al.\ 2026 \quad$\cdot$\quad github.com/xoubish/JAISP \quad$\cdot$\quad shemmati@caltech.edu}
\end{frame}

\end{document}
